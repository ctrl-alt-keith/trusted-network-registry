# Terraform Consumer: Generated tfvars JSON

The publisher can emit `trusted-registry.auto.tfvars.json`. Terraform loads
that file automatically when it is placed in the root module directory.

The generated file contains:

- `trusted_registry`
- `trusted_admin_cidrs`
- `trusted_registry_valid_until`

Keep generated tfvars JSON private. It is derived from sensitive registry data.
